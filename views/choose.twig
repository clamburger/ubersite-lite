{% extends "master.twig" %}

{% block content %}
  {% if questionnaires | length > 0 %}
    <ul>
    {% for questionnaire in questionnaires %}
      <li><a href="{{ redirect }}/{{ questionnaire.Id }}">{{ questionnaire.Name }}</a></li>
    {% endfor %}
    </ul>
  {% else %}
    There are currently no questionnaires available.
  {% endif %}
{% endblock %}
